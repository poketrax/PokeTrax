export class Set{
    public name: string
    public id: string
    public count: number

    constructor(id: string, name: string, count: number){
        this.id =id
        this.name = name
        this.count =count
    }
}